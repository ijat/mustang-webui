# Go build output
/bin/

# Provisioned runtime — see internal/appdir. Created next to the binary
# at run time (also lands at repo root during `go run`/local dev).
/mustang-webui-data/

# Maven build output
sidecar/target/
sidecar/dependency-reduced-pom.xml

# Node
web/frontend/node_modules/
web/frontend/.vite/

# web/dist is intentionally NOT ignored — see CLAUDE.md. It's the embedded
# frontend build; `make build-frontend` regenerates and commits it.

# OS / editor cruft
.DS_Store
*.swp
